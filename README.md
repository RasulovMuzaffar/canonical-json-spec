# Canonical JSON Specification

Этот репозиторий содержит **референс-реализацию Canonical JSON**  
на нескольких языках программирования:

- Java
- C#
- Python
- Go
- PHP

Canonical JSON — это алгоритм преобразования произвольной JSON-структуры  
в **строго детерминированное строковое представление**, одинаковое на всех платформах  
и для любых реализаций, при условии, что входные данные совпадают.

---

## 🎯 Назначение

Canonical JSON обеспечивает:

- предсказуемый порядок ключей в объектах;
- одинаковый вывод независимо от языка и библиотеки сериализации;
- компактный (minified) формат без лишних пробелов и переносов;
- стабильность структуры для бинарного сравнения или хэширования.

Такое детерминированное представление применяется в задачах:

- вычисление криптографических хэшей (SHA-256, SHA-512 и др.);
- создание и проверка цифровых подписей (RSA, ECDSA, EdDSA и др.);
- контроль целостности данных в распределённых системах;
- формирование устойчивых идентификаторов объектов и ключей кэша;
- сравнение JSON-структур на идентичность;
- ведение журналов и аудита, требующих точного бинарного соответствия.

Canonical JSON не зависит от какого-либо конкретного протокола  
и не привязан к определённой архитектуре.  
Это **универсальная утилита**, подходящая для любых интеграционных и криптографических систем.

---

## 📐 Алгоритм Canonical JSON

Пусть `V` — произвольное JSON-значение.

### 1. Объект (JSON object)
- Список ключей сортируется по лексикографическому порядку (по алфавиту).  
- Для каждого значения применяется `canonicalJson` рекурсивно.  
- Выводится как компактный JSON `{...}`.

### 2. Массив (JSON array)
- Порядок элементов сохраняется.  
- К каждому элементу применяется рекурсивная нормализация.  
- Выводится как `[...]`.

### 3. Примитивы
Строки, числа, логические значения и `null` выводятся **как есть**,  
без дополнительных преобразований.

### 4. Строковый вывод
- формат **minified JSON** (без пробелов, без переносов строк);
- Unicode символы **не экранируются**, если библиотека даёт такую возможность  
  (например `"тест"` → `"тест"`, а не `"\\u0442\\u0435\\u0441\\u0442"`).

---

## 📄 Пример

Вход (`examples/example1_input.json`):

```json
{
  "z": 1,
  "a": { "k": 2, "b": 1 },
  "list": [
    { "x": 2, "a": 1 },
    3
  ]
}
```

Результат (`examples/example1_canonical.json`):

```json
{"a":{"b":1,"k":2},"list":[{"a":1,"x":2},3],"z":1}
```
